diff --git a/dist/edge-tts.d.ts b/dist/edge-tts.d.ts
index 5d71514227e3f5d28b6f707ec7881c193a842975..ef0fe5736c3369fb1d0ae3cbe40bbe79043d3055 100644
--- a/dist/edge-tts.d.ts
+++ b/dist/edge-tts.d.ts
@@ -26,8 +26,8 @@ declare class EdgeTTS {
     private volume;
     private timeout;
     constructor({ voice, lang, outputFormat, saveSubtitles, proxy, rate, pitch, volume, timeout }?: configure);
-    _connectWebSocket(): Promise<WebSocket>;
+    _connectWebSocket(signal?: AbortSignal): Promise<WebSocket>;
     _saveSubFile(subFile: subLine[], text: string, audioPath: string): void;
-    ttsPromise(text: string, audioPath: string): Promise<unknown>;
+    ttsPromise(text: string, audioPath: string, signal?: AbortSignal): Promise<unknown>;
 }
 export { EdgeTTS };
diff --git a/dist/edge-tts.js b/dist/edge-tts.js
index fc6c84090696e013411686f4ef1d394248f9be97..d7d499203dcacf1a6c590447ebebf979b0931a7b 100644
--- a/dist/edge-tts.js
+++ b/dist/edge-tts.js
@@ -30,7 +30,7 @@ class EdgeTTS {
         this.volume = volume;
         this.timeout = timeout;
     }
-    async _connectWebSocket() {
+    async _connectWebSocket(signal) {
         const wsConnect = new ws_1.WebSocket(`wss://speech.platform.bing.com/consumer/speech/synthesize/readaloud/edge/v1?TrustedClientToken=${drm_1.TRUSTED_CLIENT_TOKEN}&Sec-MS-GEC=${(0, drm_1.generateSecMsGecToken)()}&Sec-MS-GEC-Version=1-${drm_1.CHROMIUM_FULL_VERSION}`, {
             host: 'speech.platform.bing.com',
             origin: 'chrome-extension://jdiccldimpdaibmpdkjnbmckianbfold',
@@ -44,8 +44,27 @@ class EdgeTTS {
             agent: this.proxy ? new https_proxy_agent_1.HttpsProxyAgent(this.proxy) : undefined
         });
         return new Promise((resolve, reject) => {
-            wsConnect.on('open', () => {
-                wsConnect.send(`Content-Type:application/json; charset=utf-8\r\nPath:speech.config\r\n\r\n
+            let settled = false;
+            const cleanup = () => {
+                signal?.removeEventListener('abort', onAbort);
+                wsConnect.removeListener('open', onOpen);
+                wsConnect.removeListener('error', onError);
+            };
+            const rejectOnce = (error) => {
+                if (settled)
+                    return;
+                settled = true;
+                cleanup();
+                wsConnect.terminate();
+                reject(error);
+            };
+            const onAbort = () => rejectOnce(signal?.reason ?? new DOMException('Aborted', 'AbortError'));
+            const onError = (error) => rejectOnce(error);
+            const onOpen = () => {
+                if (settled)
+                    return;
+                try {
+                    wsConnect.send(`Content-Type:application/json; charset=utf-8\r\nPath:speech.config\r\n\r\n
           {
             "context": {
               "synthesis": {
@@ -60,11 +79,21 @@ class EdgeTTS {
             }
           }
         `);
+                }
+                catch (error) {
+                    rejectOnce(error);
+                    return;
+                }
+                settled = true;
+                cleanup();
                 resolve(wsConnect);
-            });
-            wsConnect.on('error', (err) => {
-                reject(err);
-            });
+            };
+            signal?.addEventListener('abort', onAbort, { once: true });
+            wsConnect.once('open', onOpen);
+            wsConnect.once('error', onError);
+            if (signal?.aborted) {
+                onAbort();
+            }
         });
     }
     _saveSubFile(subFile, text, audioPath) {
@@ -91,13 +120,54 @@ class EdgeTTS {
         });
         (0, node_fs_1.writeFileSync)(subPath, JSON.stringify(subFile, null, '  '), { encoding: 'utf-8' });
     }
-    async ttsPromise(text, audioPath) {
-        const _wsConnect = await this._connectWebSocket();
+    async ttsPromise(text, audioPath, signal) {
+        signal?.throwIfAborted();
+        const _wsConnect = await this._connectWebSocket(signal);
         return new Promise((resolve, reject) => {
             let audioStream = (0, node_fs_1.createWriteStream)(audioPath);
             let subFile = [];
-            let timeout = setTimeout(() => reject('Timed out'), this.timeout);
-            _wsConnect.on('message', async (data, isBinary) => {
+            let settled = false;
+            const cleanup = () => {
+                clearTimeout(timeout);
+                signal?.removeEventListener('abort', onAbort);
+                _wsConnect.removeListener('message', onMessage);
+                _wsConnect.removeListener('error', onSocketError);
+                _wsConnect.removeListener('close', onSocketClose);
+                audioStream.removeListener('error', onStreamError);
+            };
+            const fail = (error) => {
+                if (settled)
+                    return;
+                settled = true;
+                cleanup();
+                audioStream.destroy();
+                _wsConnect.terminate();
+                reject(error);
+            };
+            const succeed = () => {
+                if (settled)
+                    return;
+                settled = true;
+                cleanup();
+                _wsConnect.close();
+                try {
+                    if (this.saveSubtitles) {
+                        this._saveSubFile(subFile, text, audioPath);
+                    }
+                    resolve();
+                }
+                catch (error) {
+                    reject(error);
+                }
+            };
+            const onAbort = () => fail(signal?.reason ?? new DOMException('Aborted', 'AbortError'));
+            const onSocketError = (error) => fail(error);
+            const onSocketClose = () => fail(new Error('Edge TTS websocket closed before synthesis completed'));
+            const onStreamError = (error) => fail(error);
+            const timeout = setTimeout(() => fail(new Error('Timed out')), this.timeout);
+            const onMessage = (data, isBinary) => {
+                if (settled)
+                    return;
                 if (isBinary) {
                     let separator = 'Path:audio\r\n';
                     let index = data.indexOf(separator) + separator.length;
@@ -108,14 +178,7 @@ class EdgeTTS {
                     let message = data.toString();
                     if (message.includes('Path:turn.end')) {
                         audioStream.end();
-                        audioStream.on('finish', () => {
-                            _wsConnect.close();
-                            if (this.saveSubtitles) {
-                                this._saveSubFile(subFile, text, audioPath);
-                            }
-                            clearTimeout(timeout);
-                            resolve();
-                        });
+                        audioStream.once('finish', succeed);
                     }
                     else if (message.includes('Path:audio.metadata')) {
                         let splitTexts = message.split('\r\n');
@@ -132,9 +195,19 @@ class EdgeTTS {
                         catch { }
                     }
                 }
-            });
+            };
+            signal?.addEventListener('abort', onAbort, { once: true });
+            _wsConnect.on('message', onMessage);
+            _wsConnect.once('error', onSocketError);
+            _wsConnect.once('close', onSocketClose);
+            audioStream.once('error', onStreamError);
+            if (signal?.aborted) {
+                onAbort();
+                return;
+            }
             let requestId = (0, node_crypto_1.randomBytes)(16).toString('hex');
-            _wsConnect.send(`X-RequestId:${requestId}\r\nContent-Type:application/ssml+xml\r\nPath:ssml\r\n\r\n
+            try {
+                _wsConnect.send(`X-RequestId:${requestId}\r\nContent-Type:application/ssml+xml\r\nPath:ssml\r\n\r\n
       ` + `<speak version="1.0" xmlns="http://www.w3.org/2001/10/synthesis" xmlns:mstts="https://www.w3.org/2001/mstts" xml:lang="${this.lang}">
         <voice name="${this.voice}">
           <prosody rate="${this.rate}" pitch="${this.pitch}" volume="${this.volume}">
@@ -142,6 +215,10 @@ class EdgeTTS {
           </prosody>
         </voice>
       </speak>`);
+            }
+            catch (error) {
+                fail(error);
+            }
         });
     }
 }
